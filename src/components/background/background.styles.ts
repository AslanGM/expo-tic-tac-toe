import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    bg: {
        position: 'absolute',
        top: 0,
        left: 0,
        width: '100%',
        height: '100%',
        backgroundColor: '#327B6A'
    }
})

export default styles
