import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    container: {
        alignItems: 'center',
        paddingTop: 120
    },
    logo: {
        width: '60%',
        height: 120,
        resizeMode: 'contain'
    },
    buttonsContainer: {
        marginTop: 40,
        padding: 20,
        width: '100%',
        gap: 12
    }
})

export default styles
